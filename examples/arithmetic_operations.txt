

main {
    int a, b, c, result;
    float x, y, z;
    

    a = 5;
    b = 10;
    c = 3;
    
 
    result = a + b * c;       
    result = (a + b) * c;     
    result = a * b / c + a;    
    
  
    result = a + b * c - a / c + 10 * (b - a);
    

    x = 3.14;
    y = 2.5;
    z = x * y + c;             
    
    
    z = x + a;                 
    
    
    result = a ^ 2;          
    
 
    cout << result;
}

